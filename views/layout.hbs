<!DOCTYPE html>
{{! prettier-ignore }}
<html lang="en">
  <head>
    <title>Members Only | {{title}}</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container-fluid bg-gradient bg-light">
      <div class="container">
        <header class="row justify-content-between pb-2 pt-4 mb-4">
          <a href="/" class="col-md-4 col-12 text-dark text-decoration-none d-flex justify-content-center justify-content-md-start mb-3 mb-0-md">
            <span class="fs-3">Members Only</span>
          </a>
          <ul class="col-md-8 col-12 nav nav-pills d-flex justify-content-center justify-content-md-end mb-2 mb-0-md">
            <li class="nav-item"><a href="/" class="nav-link{{#if messagesPage}} active{{/if}}" aria-current="page">Messages</a></li>
            {{#if user}}
              {{#unless user.member}}
                <li class="nav-item"><a href="/join-the-club" class="nav-link{{#if joinTheClubPage}} active{{/if}}">Join the club</a></li>
              {{/unless}}
              <li class="nav-item"><a href="/sign-out" class="nav-link">Sign Out</a></li>
            {{else}}
              <li class="nav-item"><a href="/sign-in" class="nav-link{{#if signInPage}} active{{/if}}">Sign In</a></li>
              <li class="nav-item"><a href="/sign-up" class="nav-link{{#if signUpPage}} active{{/if}}">Sign Up</a></li>
            {{/if}}
          </ul>
        </header>
      </div>
    </div>
    <div class="container">{{{body}}}</div>
    {{#if user}}
      <footer class="py-5 mt-5 text-center fw-light text-muted">
        <p>Membership status: {{#if user.member}}<span class="text-success">Member</span>{{else}}<span class="text-danger">Non-member</span>{{/if}}</p>
      </footer>
    {{/if}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>
  </body>
</html>